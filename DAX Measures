1. Total Sales:
Total Sales = SUM('Dataset'[ Sales])


2. Total Profit:
Total Profit = SUM('Dataset'[Profit])


3. Unit Sold:
Unit Sold = SUM('Dataset'[Units Sold])


4. COGS:
    COGS % = 
    VAR COGS = SUM('Dataset'[COGS])
    VAR COGS_Per = DIVIDE(COGS, [Total Sales],0)
RETURN
    COGS_Per


5. Discounts:
Discounts = SUM('Dataset'[Discounts])


6. Gross Profit Margin:
Gross Profit Margin = DIVIDE([Total Profit], [Total Sales])
